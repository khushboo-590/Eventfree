import React from 'react'

const
    Header = ({ title }) => {
        return (
            <h2 className='font-bold  '>
                {title}
            </h2>
        )
    }

export default Header

